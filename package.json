{
  "name": "hornbeam",
  "workspaces": [
    "client",
    "common",
    "server"
  ],
  "scripts": {
    "create-database": "npm run --workspace server create-database",
    "test": "cd client && npm test && cd ../server && npm test"
  },
  "dependencies": {
    "@types/mocha": "^10.0.7"
  }
}
