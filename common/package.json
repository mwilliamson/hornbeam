{
  "name": "hornbeam-common",
  "scripts": {
    "prepare": "npm run build",
    "build": "tsc",
    "build-watch": "tsc --watch",
    "check": "npm run check:eslint",
    "check:eslint": "eslint src && echo 'No lint errors' 1>&2",
    "test": "NODE_OPTIONS=--enable-source-maps mocha"
  },
  "type": "commonjs",
  "mocha": {
      "spec": "lib/**/*.test.js"
  }
}
