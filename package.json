{
  "name": "hornbeam",
  "workspaces": [
    "client",
    "common",
    "server"
  ],
  "scripts": {
    "create-database": "npm run --workspace server create-database",
    "test": "npm test --workspace server && npm test --workspace client"
  },
  "dependencies": {
    "@types/mocha": "^10.0.7"
  }
}
